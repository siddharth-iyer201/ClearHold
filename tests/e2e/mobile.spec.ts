import {test,expect} from '@playwright/test';test('portfolio and calculation trail remain usable at 390px',async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto('/');await page.getByLabel('Portfolio positions').fill('IVV 12000\nSUSA 5000\nXOM 1500\nUNKNOWN 1500');await page.getByRole('button',{name:/Analyze my portfolio/}).click();await expect(page.getByText('ANALYSIS COVERAGE')).toBeVisible();await page.getByRole('button',{name:/Show calculation trail/}).click();await expect(page.getByText(/position dollars × company weight/)).toBeVisible();await expect(page.locator('body')).not.toHaveCSS('overflow-x','scroll');await page.locator('#portfolio').screenshot({path:'public/screenshots/clearhold-mobile.png'});});
