import {test,expect} from '@playwright/test';test('analyzes a fund and renders the swap',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:/Do you know what you own/})).toBeVisible();await expect(page.getByText('PORTFOLIO X-RAY')).toBeVisible();await expect(page.getByText('Largest flagged holdings')).toBeVisible();await page.getByLabel('Investment amount').fill('25000');await expect(page.getByText('on a $25,000 position')).toBeVisible();await expect(page).toHaveScreenshot('clearhold-home.png',{fullPage:true,animations:'disabled',maxDiffPixelRatio:.02})});
